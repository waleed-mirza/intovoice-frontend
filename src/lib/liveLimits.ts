/** Hard cap for a single live broadcast (59 minutes). */
export const MAX_LIVE_DURATION_SECONDS = 59 * 60;

/** Show remaining-time countdown once this much time is left (5 minutes). */
export const LIVE_COUNTDOWN_WINDOW_SECONDS = 5 * 60;
