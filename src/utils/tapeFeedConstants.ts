/** Tapes loaded per API page across grid views and the vertical feed. */
export const TAPE_FEED_PAGE_SIZE = 10;

/** Prefetch the next page when this many tapes remain (incl. current). ~7th tape in a batch of 10. */
export const TAPE_FEED_PREFETCH_REMAINING = 4;
